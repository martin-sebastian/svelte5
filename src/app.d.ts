/// <reference types="@sveltejs/kit" />
/// <reference types="svelte" />

declare global {
	namespace App {
		interface Locals {
			// Empty for now
		}
		interface PageData {
			// Empty for now
		}
		// interface Error {}
		// interface Platform {}
	}
}

export {};
